##################################
###	Box Volume  ##################
##################################

# Computes the volume of a box given 3 positive integers representing length, width and height of a box.

length = float(raw_input('Enter length(cm): '))
width = float(raw_input('Enter width(cm): '))
height = float(raw_input('Enter height(cm): '))

print 'Volume =', length * width * height